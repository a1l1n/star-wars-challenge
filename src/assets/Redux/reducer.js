
const initialState = {
    films: [],
    characters: [],
    filteredCharacter: []
};

export default function reducer(state = initialState, actions) {
    switch(actions.type) {
        
        default: return state;
    }
}